import { CreatePostClient } from "./CreatePostClient";

export const metadata = { title: "Share Your Color Story — 144,000 Color Project" };

export default function CreatePostPage() {
  return <CreatePostClient />;
}
